#Sample Code Run
1. Download the folder
2. If you are on Mac run the command 'sudo npm install -g jest' at the root level
   to install jest command line
3. Run the command 'jest' to run the test suite. The test will evaluate the js file
   and return the results. 
